# .config/tmux

tmux configuration.


## Installation

Install tmux:

```bash
brew install tmux
```

Symlink this configuration from `~/.config`:

```bash
ln -s "${HOME}/.config/tmux/.tmux.conf" "${HOME}/.tmux.conf"
```
